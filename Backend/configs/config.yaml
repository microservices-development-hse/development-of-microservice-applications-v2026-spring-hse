ProgramSettings:
  resourceTimeout: 5
  analyticsTimeout: 15
  bindAddress: "0.0.0.0"
  bindPort: 8000

DBSettings:
  dbUser: "postgres"
  dbPassword: "yourpassword"
  dbHost: "localhost"
  dbPort: 5432
  dbName: "postgres"

CorsSettings:
  allowedOrigins: 
    - "http://localhost:4200"
    - "http://localhost:8000"
  allowedMethods:
    - "GET"
    - "POST"
    - "PUT"
    - "DELETE"
    - "OPTIONS"
  allowedHeaders:
    - "Accept"
    - "Authorization"
    - "Content-Type"
    - "X-CSRF-Token"